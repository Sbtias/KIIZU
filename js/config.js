// Supabase public client configuration.
// Replace these placeholders locally. Never expose a service-role key in the browser.
export const SUPABASE_URL = window.KIIZU_SUPABASE_URL || '';
export const SUPABASE_ANON_KEY = window.KIIZU_SUPABASE_ANON_KEY || '';